# Extension API boundary

No runtime plugin loading is implemented. A later installed Forge mod may register a versioned namespaced provider only during controlled startup, declaring side, thread, permission, cost and bounded outputs. GradleMC must not scan a folder for JARs, download code, execute scripts, reflect arbitrary classes or expose mutable internals.
