# v1.0.4 Phase 3 verification

Implemented in this increment: staged overlay settings (Apply, Cancel, Escape/Back discard, staged defaults, clamping and save-failure retention) and bounded deterministic task-ID/dependency traversal checks.

Java 17.0.17 gates passed: `compileJava`, `test`, `gradlemcSelfTest`, `build`, and `runGameTestServer`. `runServer` and `runClient` were started for bounded smoke attempts but remained interactive; their verified project-owned Java processes were stopped and no visual claim is made. The requested in-app Browser was unavailable. No project-owned Java process remained after each stop.

The local verification artifact is `build/libs/gradlemc-1.0.4-forge-1.20.1.jar`, SHA-256 `0338F3B65A42303B70383329207616503B7D5D6A1FFDDEB4AD60C14A410DB742`. It was not exported.

## Phase 3C continuation (2026-07-24)

Phase 3C is incomplete. It adds the initial bounded Performance Guard/mode foundation, removes confirmed overlay per-frame configuration/window work and repeated unchanged-line width work, and updates static task fingerprints to length-prefixed full SHA-256. Java 17 `compileJava`, `test`, and `gradlemcSelfTest` passed after one transient ForgeGradle TLS certificate-validation failure on an earlier combined attempt. The requested `build` attempt then stopped before tasks on TLS validation for `maven.minecraftforge.net`. Persistent cache/history, generic measurement hub, executor consolidation, JFR, runtime smoke, and release verification remain.
