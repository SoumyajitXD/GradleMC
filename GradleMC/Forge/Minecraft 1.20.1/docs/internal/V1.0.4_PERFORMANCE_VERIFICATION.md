# v1.0.4 Phase 3C performance verification

Completed: overlay FPS sampling no longer calls `setWindowSeconds` per frame; unchanged overlay lines retain their measured layout; explicit setting changes invalidate cached presentation. Static fingerprints are length-prefixed full SHA-256. A bounded hysteretic Performance Guard and persisted mode foundation were added with permission-gated commands.

Java 17 `compileJava`, `test`, and `gradlemcSelfTest` passed. One earlier combined test invocation stopped at transient ForgeGradle certificate validation before task execution; independent retries passed. `build` was attempted and stopped during ForgeGradle configuration when TLS certificate validation for `maven.minecraftforge.net` failed, before build tasks ran. GameTests, smoke, JFR, GUI visual matrix, executor consolidation, generic subscriptions, and persistent Phase 3B cache/history remain incomplete.
